---
# 2026-07-09 통합본: 같은 날짜 발행 2편(로봇 데이터 드릴 + import 사건)을 1편으로 합침 (하루 1편 원칙).
publish: true
title: "float32 비교와 import 실행 모델 — 파이썬이 조용히 틀리는 자리"
date: 2026-07-09T12:15:00+09:00
tags:
  - 파이썬
  - Python
  - LeRobot
  - float32
  - import
  - 데이터검증
description: "공개 데이터셋 lerobot/pusht로 드릴을 만들며 정리한 float32 부동소수점 비교와 파이썬 import 실행 모델."
---

문법 드릴만으로는 실제 데이터의 형태를 알기 어려워서, 뒤 커리큘럼에서 다룰 로봇 시연 데이터를 미리 열어 검증 드릴 12문제를 만들었어요. 그 과정에서 교과서 예제로는 안 드러나는 두 지점, **부동소수점 비교와 파이썬 import의 실행 모델**을 정리하게 됐어요.

<div class="specs">
<span><b>데이터셋</b>lerobot/pusht · 모방학습용 시연</span>
<span><b>규모</b>에피소드 206 · 프레임 25,650 · 10 fps</span>
<span><b>형식</b>수치 Parquet · 영상 MP4 · 메타 JSON</span>
<span><b>재현</b>리비전 고정 + 문제마다 assert</span>
</div>

## 데이터 구조는 실물로 확인해요

HuggingFace에 공개된 [`lerobot/pusht`](https://huggingface.co/datasets/lerobot/pusht)를 썼어요. 로봇이 T자 블록을 미는 시연(demonstration)을 모은 데이터셋이고, 각 프레임은 관측(observation)과 행동(action)의 쌍이에요. 모방학습은 이 쌍에서 "이 관측이면 이 행동"을 지도학습으로 배워요.

참고 자료는 에피소드마다 Parquet 파일이 하나씩 있는 구조라고 했는데, 실제 데이터셋은 v3.0으로 개편되면서 청크 파일 하나에 206개가 통합돼 있었어요. 오픈소스 로보틱스 스택은 개정이 잦아 몇 달 전 자료도 어긋나기 쉬우니, **구조는 문서로 익히되 최종 확인은 실물을 열어서** 해요. 문서와 데이터가 다르면 데이터가 맞으니까요.

## float32 타임스탬프의 == 비교 오류

10 fps 데이터니까 인접 프레임의 타임스탬프 간격은 0.1초여야 하는데, `간격 == 0.1`로 검증했더니 전부 실패했어요.

<div class="chart">
<div class="t">같은 0.1인데 값이 달라요</div>
<div class="sub">근사에 쓰는 비트 수가 타입마다 달라서, 같은 십진수도 서로 다른 2진 근사값이 돼요.</div>
<svg viewBox="0 0 880 250" width="100%" style="max-width:880px" role="img" aria-label="0.1을 float32와 float64가 서로 다른 값으로 근사해 정확 비교가 깨진다"><text x="110" y="28" font-size="11.5" fill="var(--darkgray)">0.1은 2진수로 유한하게 안 떨어져요 — 0.0001100110011… 무한 반복</text><text x="832" y="28" text-anchor="end" font-size="10.5" fill="var(--gray)">수직선을 크게 확대한 구간이에요</text><line x1="110" y1="76" x2="832" y2="76" stroke="var(--darkgray)" stroke-width="1.6"/><text x="110" y="66" font-size="10.5" font-family="ui-monospace,monospace" fill="var(--gray)">0.09999999</text><text x="832" y="66" text-anchor="end" font-size="10.5" font-family="ui-monospace,monospace" fill="var(--gray)">0.10000001</text><line x1="369.9" y1="76" x2="644.3" y2="76" stroke="var(--sv-bad)" stroke-width="5" opacity="0.3"/><line x1="369.9" y1="67" x2="369.9" y2="96" stroke="var(--sv-ok)" stroke-width="1.4" stroke-dasharray="3 3"/><circle cx="369.9" cy="76" r="5.5" fill="var(--sv-ok)"/><text x="369.9" y="112" text-anchor="middle" font-size="11.5" fill="var(--sv-ok)">파이썬 리터럴 0.1</text><text x="369.9" y="128" text-anchor="middle" font-size="10" font-family="ui-monospace,monospace" fill="var(--gray)">0.1000000000000000055…</text><text x="369.9" y="143" text-anchor="middle" font-size="10" fill="var(--gray)">float64 · 가수 52비트</text><line x1="644.3" y1="67" x2="644.3" y2="160" stroke="var(--sv-bad)" stroke-width="1.4" stroke-dasharray="3 3"/><circle cx="644.3" cy="76" r="5.5" fill="var(--sv-bad)"/><text x="644.3" y="176" text-anchor="middle" font-size="11.5" fill="var(--sv-bad)">데이터셋 timestamp 간격</text><text x="644.3" y="192" text-anchor="middle" font-size="10" font-family="ui-monospace,monospace" fill="var(--gray)">0.10000000149…</text><text x="644.3" y="207" text-anchor="middle" font-size="10" fill="var(--gray)">float32 · 가수 23비트</text><text x="507.1" y="52" text-anchor="middle" font-size="10.5" fill="var(--sv-bad)">두 값이 달라서 == 가 깨져요</text><text x="110" y="240" font-size="11" fill="var(--darkgray)">그래서 정확히 같은지가 아니라 허용 오차 안에서 같은지로 비교해요 — <tspan font-family="ui-monospace,monospace" fill="var(--sv-ok)">math.isclose(gap, 0.1, abs_tol=1e-6)</tspan></text></svg>
</div>

timestamp는 float32(가수 23비트)라 0.1이 `0.10000000149…`가 됐고, 파이썬 리터럴 `0.1`은 float64(가수 52비트)라 더 촘촘한 다른 근사값이에요. 두 값이 다르니 `==`가 깨져요.

```python
import math
gap == 0.1                            # 실패: 정확 비교
math.isclose(gap, 0.1, abs_tol=1e-6)  # 수정: 허용 오차
```

`abs_tol`은 절대 허용 오차예요. `isclose`는 기본적으로 상대 오차(`rel_tol`)로 비교하는데, **0에 가까운 값에서는 그것만으로 부족해서** 절대 오차를 함께 줘요.

## import는 모듈의 최상위 코드를 실행해요

모듈을 하나 만들고 아래쪽에 테스트용 `print`를 뒀는데, 다른 파일에서 import하는 순간 그 `print`가 실행됐어요.

<div class="chart">
<div class="t">import가 실제로 하는 일</div>
<div class="sub">최상위에 부작용(side effect) 코드를 두면 import만 해도 돌아가요.</div>
<svg viewBox="0 0 880 268" width="100%" style="max-width:880px" role="img" aria-label="import는 모듈 최상위 코드를 실행하고 name 가드 안쪽만 건너뛴다"><rect x="24" y="40" width="330" height="196" fill="none" stroke="var(--lightgray)"/><text x="36" y="32" font-size="11.5" font-family="ui-monospace,monospace" fill="var(--dark)">mymodule.py</text><text x="38" y="68" font-size="11" font-family="ui-monospace,monospace" fill="var(--darkgray)">import numpy as np</text><text x="366" y="68" font-size="10.5" fill="var(--sv-ok)">실행됨</text><text x="38" y="96" font-size="11" font-family="ui-monospace,monospace" fill="var(--darkgray)">def load(): ...</text><text x="366" y="96" font-size="10.5" fill="var(--sv-ok)">정의 실행됨</text><text x="38" y="124" font-size="11" font-family="ui-monospace,monospace" fill="var(--darkgray)">class Robot: ...</text><text x="366" y="124" font-size="10.5" fill="var(--sv-ok)">정의 실행됨</text><text x="38" y="152" font-size="11" font-family="ui-monospace,monospace" fill="var(--sv-bad)">print('여기도 돕니다')</text><text x="366" y="152" font-size="10.5" fill="var(--sv-bad)">실행됨 ← 부작용</text><text x="38" y="180" font-size="11" font-family="ui-monospace,monospace" fill="var(--darkgray)">if __name__ == '__main__':</text><text x="38" y="208" font-size="11" font-family="ui-monospace,monospace" fill="var(--gray)">    run_tests()</text><text x="366" y="208" font-size="10.5" fill="var(--gray)">건너뜀</text><rect x="30" y="164" width="318" height="46" fill="var(--sv-ok)" opacity="0.08"/><line x1="510" y1="52" x2="510" y2="224" stroke="var(--lightgray)"/><text x="534" y="70" font-size="12" font-weight="640" fill="var(--dark)">import mymodule 하면</text><text x="534" y="96" font-size="11" fill="var(--darkgray)">· 최상위 코드를 위에서 아래로 한 번 실행</text><text x="534" y="120" font-size="11" fill="var(--darkgray)">· 함수·클래스 정의문도 그때 실행</text><text x="534" y="144" font-size="11" fill="var(--darkgray)">· 그 사이 일반 문장도 똑같이 실행</text><text x="534" y="168" font-size="11" fill="var(--darkgray)">· <tspan fill='var(--sv-ok)'>__main__ 가드 안쪽만 건너뜀</tspan></text><text x="534" y="192" font-size="11" fill="var(--darkgray)">· 한 번 실행되면 sys.modules에 캐시돼 재실행 안 됨</text></svg>
</div>

`__name__`은 파일을 `python x.py`로 직접 실행하면 `'__main__'`, 다른 파일이 `import x`로 부르면 모듈 이름 `'x'`가 돼요. 그래서 가드 안의 코드는 직접 실행에서만 돌고 import에서는 건너뛰어요.

```python
if __name__ == '__main__':
    # 파일을 직접 실행할 때만 도는 코드
```

## 조용한 실패는 한 얼굴이에요

드릴에서 반복해 걸린 실수들의 원인이 하나로 모여요. **파이썬은 오류로 멈추지 않고 조용히 다른 값을 돌려준다**는 점이에요.

| 쓴 것 | 기대한 결과 | 실제 결과 |
|---|---|---|
| `return print(...)` | 값이 반환됨 | `print`는 값을 반환하지 않아 `None` |
| `min(xs, ys)` | 각 리스트의 최솟값 | 두 시퀀스를 사전순 비교한 결과 |
| 컴프리헨션에 `append` 혼용 | 리스트가 채워짐 | 세 자리를 지키면 `append`가 아예 불필요 |
| `gap == 0.1` | 타임스탬프 검증 통과 | float32/float64 근사가 달라 전부 실패 |
| 모듈 최상위 `print` | 직접 실행할 때만 출력 | import만 해도 실행 |

정적 타입 언어라면 컴파일 단계에서 걸렸을 것들이 파이썬에서는 런타임에 조용히 통과하니, `__main__` 가드나 assert 셀프체크 같은 **방어 관용구**가 자주 쓰여요.

## 드릴 구성과 재현 장치

데이터 검증(관측 키 추출, 프레임 수 집계, 타임스탬프 간격 검증, 좌표 범위 확인)과 문법 연습(컴프리헨션·`Counter`·`deque`)을 합쳐 12문제를 만들었어요. 재현을 위해 데이터셋 리비전(커밋 해시)을 고정했고, 문제마다 붙인 assert가 실행 즉시 통과·실패를 찍어요. 전체 드릴(58문제 + 답안)은 [robotics-python-drills](https://github.com/mmporong/robotics-python-drills)에 공개해 뒀어요.

전날 로봇의 몸(모터·실시간 신호)을 배운 기록은 [[2026-07-08_로봇의_시간|1kHz 제어 주기와 지터 — 로봇 제어가 1ms를 다투는 이유]]에 있어요.
