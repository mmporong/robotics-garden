# robotics-garden 발행 글 스타일 가이드

이 가든은 **학습 기록·복습·기술 블로그**다. AI틱한 문체와 불필요한 어투를 배제한다.
메인 레퍼런스: **토스 기술블로그(toss.tech)**. 확정 예시(파일럿): `content/physical-ai/2026-07-09_로봇데이터_드릴.md` — 이 글의 톤·구조·밀도를 기준으로 삼는다. (확정: 2026-07-14)

## 목소리
- 토스체 존댓말(~요/~어요), 담백한 온도.
- **장난스러운 구어체 금지** ("붕 떴어요" 류).
- **경험·기분이 본문이 되면 안 된다.** 금지 예: "확 와닿았어요", "몸으로 이해했어요", "솔직히 백지에서 바로 안 나와요". 감정 채색·개인 진행상황을 걷어내고 학습 내용을 본문으로.

## 내용 (학습 내용이 본문)
- 정식 용어·영문 병기가 본문. 비유는 최대 보조 1줄, 또는 생략.
- 각 섹션은 **정의 → 원리 → 증상/예시**로 완결한다. TODO("확인 필요")로 미루지 않는다.
- 증거 중심: 수치·코드·콘솔 출력·리비전 고정·assert 등 재현 가능한 근거.
- 재료가 되면 문제 제시 → 해결 → 검증 흐름으로.
- **현업 층위를 한 겹 얹는다** (2026-08-13 사용자 지시). 무엇이 안 됐는지만 쓰면 증상 일지가 된다. 과하지 않게 — 절마다 한두 문장이나 표 한 줄이면 된다.
  - ROS가 재료면 **구성**을 적는다: 어떤 노드가 무엇을 발행·구독했고 토픽 이름·QoS·주기가 무엇이며 어디서 끊겼는지. "통신이 안 됐다"가 아니라 "`/scan`은 BEST_EFFORT 10 Hz로 오는데 `/tf`는 RELIABLE이라 P90 2.6초로 밀렸다".
  - 알고리즘이 재료면 **선택지와 근거**를 적는다: 그 자리에서 쓰이는 방법이 무엇무엇인지(경로 추종 = Pure Pursuit·Stanley·MPC / 위치추정 = AMCL 파티클 필터 / 정합 = scan matching·포즈 그래프 최적화), 무엇을 골랐고 왜 골랐고, 그 선택이 어떤 파라미터로 드러나는지.
  - **표준 용어를 본문에 쓴다**: 관측 퇴화·포즈 그래프·코스트맵 인플레이션·역빈도 가중처럼 현직자가 검색하는 이름으로 적고 처음 나올 때 한 줄 풀이를 붙인다.
  - 하지 말 것: 안 해 본 것을 해 본 것처럼 적기(진실성 게이트가 최우선) · 교과서 요약 나열 · 대안 알고리즘 카탈로그.

## 걷어낼 것 (2026-08-11 전수 점검으로 추가)

아래 넷은 내용이 없으면서 자리만 차지한다. 쓰지 않는다.

- **메타 발화 금지** — 글이 자기 자신을 언급하는 문장. "제안으로 닫을게요", "이 글은 매주 월요일 …의 첫 편이에요", "여기서부터는 제 해석이에요", "…라는 걸 먼저 밝혀둘게요" 같은 것들. 해석임을 드러내야 하면 면책 문장을 따로 두지 말고 문장 자체를 "…로 보여요"처럼 쓴다.
- **빈 연결구 금지** — "그래도 방향은 분명해요.", "이 사례가 말하는 건 단순해요." 처럼 뒤 문장을 예고만 하고 정보가 없는 문장. 접속만 필요하면 "그래도"를 남기고 서술부는 실제 내용으로 바로 잇는다.
- **실행하지 않은 확장 사족 금지** — "회사라면 …", "사회 쪽으로 넓히면 …" 처럼 근거 없이 조직·사회로 넓히는 일반론. 이 가든은 학습 기록이라 자기 작업에 적용하는 대목이 본체다.
- **다음 편 예고 금지** — "결과는 다음 편에서 적을게요." 예고 없이 그때 쓰면 된다.

## 출처 표기 (2026-08-11 사용자 지시)

- **출처 줄에는 링크만 둔다.** 링크 텍스트로 논문 제목·도메인·한글 설명을 씌우지 않고 URL을 그대로 노출한다. 뒤에 붙이던 한글 안내 문장("본문 그림은 제작자에게 저작권이 있고…")도 쓰지 않는다.
  - 형태: `출처 — https://arxiv.org/abs/2607.11734`
- **CC BY 저작물의 라이선스 표시는 그림 캡션에 붙인다.** 재배포 요건이라 빠뜨리면 안 되고, 그림 옆이 표시 위치로도 정확하다.
  - 형태: `<span class="cap-src">(출처: Dou et al., NeuralActuator · CC BY 4.0)</span>`
  - arXiv 비독점 배포 라이선스는 재배포 허가가 아니라 인용에 기대는 것이라 라이선스 표시 대상이 아니다. 캡션의 출처 표시로 충분하다.

## 형식
- **제목**: **글의 내용을 전문 용어로 정확히 요약한다** (2026-08-11 사용자 지시로 전면 개정 — 종전 "20자 이내 호기심 후킹" 규칙은 폐기). 제목만 읽고도 무엇을 다룬 글인지, 무엇을 알아냈는지 알 수 있어야 한다.
  - **반드시 담을 것 둘**: ①대상의 정식 명칭(모델·기법·현상 이름) ②그 글의 핵심 발견. 둘 중 하나만 있으면 미달이다.
  - **길이**: 25~40자 안팎(탐색기에서 2줄). 20자 상한은 폐기했다 — 짧게 만들려다 정보를 버리는 것이 이 개정의 이유다.
  - **글 화면에서는 한 줄에 들어가야 한다** (2026-08-13 사용자 지시). 본문 폭 1120px에 `h1.article-title`이 `clamp(1.35rem, 2.4vw, 1.85rem)`이라 한 줄 예산이 **약 36em**이다. 한글 1자 ≈ 1em · 공백 ≈ 0.26em · 영숫자 1자 ≈ 0.5em으로 세면 실무 상한은 **한글 34자 + 공백 6개** 안팎이다. 40자에 가까운 제목은 공백을 뺀 한글 수를 세어 34자를 넘으면 줄인다. 크기가 뷰포트에 연동돼 있어 화면이 좁아도 접히지 않지만, 예산을 넘긴 제목은 어느 폭에서도 접힌다.
  - **종결**: 명사형으로 닫아도 된다. 해요체는 본문에만 적용한다.
  - 개정 배경: 후킹만 남은 제목("맞히는 것과 설명하는 것", "미분이 없으면 못 배워요")은 분야조차 드러나지 않아 클릭도 검색도 안 됐다. 궁금하게 만드는 것보다 **정확히 알려주는 것**을 우선한다.
  - 여전히 금지: 이모지, 장난스러운 표현, "~가 아니라 ~였어요" 통찰리빌 템플릿, 본문 내용을 넘어서는 과장.
  - 예시: "저랭크 섭동으로 배치 추론 처리량 91%를 낸 진화전략 EGGROLL" / "점유 격자의 미지·자유 이분법이 만든 frontier 탐사 실패" / "돌입 전류와 저전압 차단이 만드는 SBC 리셋"
- **미디어 최소 1개 — 모든 글에 적용** (2026-08-13 사용자 지시): 학습 노트·리서치·인사이트 전부 해당한다(인사이트는 종전대로 2개). 텍스트만으로 마치지 않는다. **"넣을 게 없다"로 넘기지 말고 만들어서라도 채운다** — 지도 PNG, 주행 궤적을 겹친 그림, 궤적이 그려지는 GIF, 계측 차트, 전후 대조 몽타주가 실기 기록의 표준 재료다. CLI 출력뿐이어도 수치를 차트로 그리면 미디어가 된다. 정말 없으면 발행 보고에 사유를 적고, 조용히 빼지 않는다.
  - 실기 기록은 **프로세스를 내리기 전에 뽑는다.** 지도와 궤적은 노드가 죽는 순간 사라진다(`map_saver`, Cartographer `/write_state`, `/trajectory_node_list`).
- **소제목**: 담백한 기술 서술형. 이모지 금지.
- **붕어빵 `## 마치며` 금지.** 마지막은 자연스럽게 닫는다.
- **bold 거의 안 씀** (핵심 판단 한 곳 정도). 이모지·형광펜식 하이라이트 금지.
- **문장 리듬**: 툭 끊기는 미완결 짧은 문장 지양. 정보를 담은 완결 문장으로 이어 쓴다. 짧은 리드인은 코드·목록 바로 앞 정도만.
- em-dash 남용 금지.
- 문단은 3~5문장 기본, 도입은 1~2문장. 문단 사이 빈 줄.
- **분량**: 기존 표준은 본문 약 1,200~3,600자(파일럿 2026-07-09 글 ≈ 2,700자). 내용이 요구하면 **그 1.5배(최대 약 5,400자)까지 허용** (2026-07-16 변경). 채워야 할 목표가 아니라 상한 — 한 글 = 한 논지 원칙은 유지하고, 논지가 둘이 되면 분할한다.
  - 다만 **1,200자는 하한이지 합격선이 아니다** (2026-07-24 추가): 본문이 1,200~1,500자에 머물면 논지가 짧은 게 아니라 근거가 빠진 경우가 대부분이다. 실효 대역은 **2,000~3,600자**(기존 발행 글의 실제 최저선이 ≈2,000자)로 보고, 미달이면 수치·한계·실패 사례를 원문에서 더 가져온다. 전례: 2026-07-23 리서치 5편이 1,215~1,425자로 나와 다음 날 전량 보강.
- 한 글 = 한 논지. 시리즈 상호 위키링크는 **`[[파일명|글 제목]]`** 형태로 앞뒤 글 연결 — 표시 텍스트에 날짜 접두사(`2026-07-14_…`)가 노출되지 않게 반드시 제목을 지정한다. 대상 글의 frontmatter `title`을 그대로 표시 텍스트로 쓴다.

## 실측 기록 글 — 위 규칙의 예외 (2026-08-13 사용자 지시)

**적용 대상 둘.** ①작업 중 만든 진단 아티팩트(HTML 기록물)를 가든으로 옮기는 글 ②실기 작업 당일의
실측·진단 기록을 처음부터 쓰는 글. 후자도 아티팩트 형식이 기본값이다 — 산문으로 풀지 말고
관문 카드·점수 카드·표를 골격으로 쓴다 (2026-08-13 사용자 지시: "이전 아티팩트처럼 작성").

이식 글은 **재작성이 아니라 이식이다** — 원본을 재료로 다시 쓰지 말고 내용을 전량 옮긴 뒤 문체만 바꾼다.

직접 쓰는 경우의 골격은 이렇다. 관문마다 `<div class="gate">`(치명적이면 `gate crit`)에
**증상 → 원인 → 조치** 세 줄을 넣고 조치 끝에 판정 태그(`tag ok` / `tag no` / `tag mid`)를 단다.
관문 사이에 그 관문의 실측값을 `<div class="scores">` 카드로 찍고, 주의·함정은 `<div class="note">`
(경고는 `note warn`, 위험은 `note bad`)로 본문에서 분리한다. 마지막 절은 관문들을 관통하는 기준
하나로 닫는다 — 관문 나열로 끝내면 다음에 쓸 수 있는 것이 남지 않는다.

이 예외를 만든 이유: 2026-08-12 복도 글에서 아티팩트 본문 10,762자가 발행본 9,420자로 줄고
`<b>` 강조 111회가 6회로, 그림 5장이 3장으로, 코드블록 3개가 1개로 깎였다. 원인은 재료 부족이 아니라
아래 세 조항이 이식을 압축으로 몰았기 때문이다.

- **분량 상한을 적용하지 않는다.** 5,400자 상한은 백지에서 쓰는 글의 기준이다. 이식 글의 분량은
  원본이 정하고, 줄일 근거가 있을 때만 줄인다.
- **강조를 원본대로 살린다.** "bold 거의 안 씀"은 이식 글에 적용하지 않는다. 실측 기록에서 굵은 글씨는
  장식이 아니라 그 줄에서 봐야 할 값을 찍는 판독선이다. 지우면 표 골격만 남고 읽는 순서가 사라진다.
- **한 글 = 한 논지를 적용하지 않는다.** 진단 기록은 관문 여러 개가 순서로 엮인 구조가 본체다.
  논지가 여럿이라고 쪼개면 "앞을 풀어야 다음이 보인다"는 정보 자체가 사라진다.

여전히 적용되는 것: 해요체 · 이모지 금지 · `## 마치며` 금지 · 제목 25~40자 전문 용어 요약형 ·
진실성 게이트 · 걷어낼 것 넷(메타 발화·빈 연결구·확장 사족·다음 편 예고).

이식 절차에서 새는 자리가 정해져 있으니 옮길 때 항목별로 대조한다.

- **그림**: 아티팩트는 base64 인라인이라 자립적이고, 가든은 `content/assets/`에 파일로 두고
  `../assets/파일명`으로 참조한다. 눈으로 옮기면 빠지므로 원본 `<figure>` 개수와 발행본 이미지 개수를 센다.
- **그림은 마크다운 대신 `<figure>`로 쓴다.** `![](…)`는 원본 폭에서 멈춰(1024px면 오른쪽이 48px 비어)
  표·카드의 오른쪽 끝과 어긋난다. 한 장은 `<figure class="fig">`, 두 장 대조는 `<figure>` 둘을
  `<div class="pair">`로 감싼다. 둘 다 본문 폭까지 늘어나 좌우 끝이 하나로 맞는다.
  형태는 `<figure class="fig"><img src="../assets/…" alt="…"><figcaption>설명</figcaption></figure>`.
  - **안에 빈 줄을 두지 않는다** — 빈 줄이 들어가면 HTML 블록이 거기서 닫혀 뒷부분이 그대로 노출된다.
  - 전역으로 이미지를 늘리지 않는 이유는 다른 글의 260~480px짜리 GIF·도식이 4배로 뭉개지기 때문이다.
    그래서 옮겨 적는 글에서만 감싸는 방식으로 둔다.
  - `.pair`는 원본 비율이 달라도 같은 상자(`max-height: 26rem` + `object-fit: contain`)에 담아 캡션 높이를 맞춘다.
- **움직이는 자료는 GIF로 — `<video>`를 쓰지 않는다** (2026-08-13 사용자 지시): 영상은 독자가 읽다 멈추고
  재생 버튼을 눌러야 보인다. GIF는 스크롤에 걸리면 그냥 돌아 흐름을 끊지 않는다. mp4를 받으면
  `python3 ~/physical-ai-lab/tools/figures/mp4_to_gif.py <입력> <출력>`으로 변환한다(상한 6 MB).
  마크업은 이미지와 같은 `<figure class="fig">`다.
- **채움 배경을 쓰지 않는다** (2026-08-13 사용자 지시): 아티팩트는 카드·박스를 옅은 색으로 채우는데
  (`note.bad`는 분홍, `gate .idx`는 회색) 그 색은 옮기지 않는다. 구분은 **테두리와 왼쪽 색 띠**로만 한다.
  `.scores`는 종전에 1px gap과 배경색으로 칸을 갈랐으므로 채움을 빼면 테두리 방식으로 바꿔야 한다.
  판정 태그(`tag ok/no/mid`)도 글자색만 남긴다.
- **라벨 자간은 0.01em.** `.scores .k` · `.note .h` · `.gate .lab`은 라틴 기준 0.07~0.1em이었는데
  한글에 걸리면 글자가 벌어져 한 낱말로 안 읽힌다.
- **코드블록만 예외로 어둡게** (`#1b1f24`, 줄번호 없음): 위의 채움 금지와 어긋나 보이지만 성격이 다르다.
  카드 배경은 색으로 의미를 칠하는 것이고, 코드는 본문과 영역을 가르는 표준 처리다.
  라이트 모드에서도 `--shiki-dark` 토큰을 쓴다.
- **ASCII 흐름도에 박스 문자(`┌ │ ─`)를 쓰지 않는다**: 본문 코드 폰트(IBM Plex Mono)에 한글도 박스 문자도
  없어 각각 다른 폰트로 대체되고, 한글은 비례폭이라 세로선이 어긋난다. `| - > v` 같은 ASCII만 쓰고,
  **정렬이 필요한 자리 뒤에 한글을 두지 않는다**(들여쓰기는 공백이라 안전하다).
- **좌우 끝은 하나여야 한다** (2026-08-13 사용자 지시): 산문에 읽기 폭을 따로 주지 않는다.
  문단만 940px에서 끊기고 표·그림·카드가 1291px까지 가면 오른쪽 끝이 두 개가 된다.
  한 줄이 길어지더라도 본문 전폭으로 흘린다.
- **코드블록**: 관문의 "조치"가 한 줄 서술로 요약되면 재현이 안 된다. 원본 `<pre>` 개수를 그대로 맞춘다.
- **차트**: 인라인 SVG는 마크다운에 그대로 들어간다(빈 줄로 감싼 한 줄짜리 블록). 다만 하드코딩 색을
  Quartz 팔레트 변수로 바꿔야 다크 모드에서 축과 눈금이 묻지 않는다. `.chart`가 시리즈 색 변수를 준다.
- **그림 속 한글은 발행 전에 눈으로 확인한다.** matplotlib은 한글 폰트를 지정하지 않으면 숫자·영문만
  대체 폰트로 그리고 한글은 두부(□)로 찍는데, 생성 로그에는 경고만 남고 그림은 정상처럼 저장된다.
  만들 때 `/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`(index=1, KR)를 지정하고,
  받은 그림은 라벨을 확대해 본다. 전례: 2026-08-12 복도 글의 몽타주 라벨 6개와 A/B 라벨 2개가
  두부로 발행됐고, 원본 스냅샷도 생성 스크립트도 안 남아 PNG 위에서 직접 복구해야 했다
  (`physical-ai-lab/tools/figures/fix_broken_hangul_labels_20260812.py`).
- **mermaid**: 가든에 없다. 코드블록 텍스트 다이어그램으로 바꾼다. 여기만 형태가 달라진다.
- **자산은 별도로 커밋한다** (2026-08-13 실제로 놓칠 뻔한 자리): `publish_garden.py`는 `learning/notes/`만
  `content/physical-ai/`로 동기화하고 **`content/assets/`는 손대지 않는다.** 스크립트가 push까지 마쳐도
  그림은 untracked 로 남아 발행된 글의 이미지가 전부 깨진다. 발행 직후 `git status --short content/assets/`로
  확인하고, 새로 넣은 파일만 경로로 나열해 커밋·push 한다(`git add .` 금지 — 공통 규칙과 동일).
- **그림은 밝은 톤으로 만든다**: 가든 본문이 밝은 바탕이라 진단용으로 쓰던 어두운 배경을 그대로 올리면
  글 가운데만 검게 뜬다. 미지 영역은 `(240,240,243)` 언저리, 자유 공간은 흰색, 벽만 진하게 둔다.
  범례는 그림 안에 넣어 캡션이 색을 설명하지 않게 한다.

## 로컬 확인
`npx quartz build --serve` → http://localhost:8080
